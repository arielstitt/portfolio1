$(() => {
    console.log("Everything is ready, let's do this");

    // ===============PORTFOLIO==============
    
    openContent = () => {
        $('.portfolio-item-1').style.display = 'block';
    }
});